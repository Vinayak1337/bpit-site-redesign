/**
 * @deprecated All data previously hardcoded here has been migrated to the database.
 * - homeHero2Data → seeded via scripts/seed-homepage.cjs (slug: 'main', key: 'HERO')
 * - homeHeroData  → legacy, no longer used
 * - homeNoticesData → legacy, no longer used
 *
 * Fetch live data using server actions in src/app/(Private Pages)/actions/
 */
